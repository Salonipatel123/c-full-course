#include <stdio.h>

int main()
{
    int principale = 100, years = 1, rate = 4;
    int Simpleinterest = (principale * rate * years) / 100;
    printf("The value of simple Interest is %d", Simpleinterest);
    return 0;
}